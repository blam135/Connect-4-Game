meta {
  name: Connect Four WebSocket API
}

auth {
  mode: none
}

docs {
  Test the Connect Four raw JSON WebSocket protocol.

  Select the Local environment before opening a request. Messages within a
  request share one WebSocket connection, but each request has its own
  connection.
}
